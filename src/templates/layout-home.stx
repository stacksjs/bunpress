<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}">
  {{ meta }}
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Custom CSS from config */
    {{ customCSS }}
  </style>
</head>
<body class="m-0 p-0 box-border font-sans antialiased bg-white">
  <main>
    {{ content }}
  </main>

  {{ scripts }}
</body>
</html>
