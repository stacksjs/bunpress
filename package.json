{
  "name": "bunpress-monorepo",
  "type": "module",
  "private": true,
  "version": "0.1.3",
  "description": "Modern documentation engine. Powered by Bun.",
  "author": "Chris Breuer <chris@stacksjs.org>",
  "license": "MIT",
  "homepage": "https://github.com/stacksjs/bunpress#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stacksjs/bunpress.git"
  },
  "bugs": {
    "url": "https://github.com/stacksjs/bunpress/issues"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run dev:docs",
    "build": "bun run build:bunpress && bun run build:plugin",
    "build:bunpress": "cd packages/bunpress && bun run build",
    "build:plugin": "cd packages/bun-plugin && bun run build",
    "compile": "cd packages/bunpress && bun run compile",
    "compile:all": "cd packages/bunpress && bun run compile:all",
    "fresh": "bunx rimraf node_modules/ packages/*/node_modules/ bun.lock && bun i",
    "prepublishOnly": "bun run build",
    "test": "bun test",
    "test:quick": "bun test --timeout=10000",
    "test:full": "bun test --timeout=60000 --no-bail",
    "lint": "bunx --bun pickier .",
    "lint:fix": "bunx --bun pickier . --fix",
    "format": "bunx --bun pickier . --format",
    "format:fix": "bunx --bun pickier . --format --write",
    "changelog": "bunx logsmith --verbose",
    "changelog:generate": "bunx logsmith --output CHANGELOG.md",
    "release": "bunx --bun bumpx prompt --recursive",
    "dev:docs": "bun packages/bunpress/bin/cli.ts dev",
    "build:docs": "bun packages/bunpress/bin/cli.ts build --outdir ./dist",
    "preview:docs": "bun packages/bunpress/bin/cli.ts preview --outdir ./dist",
    "typecheck": "bun --bun tsc --noEmit",
    "bench": "cd benchmark && bun run bench:all"
  },
  "devDependencies": {
    "@stacksjs/bumpx": "^0.2.4",
    "@stacksjs/gitlint": "^0.1.5",
    "@stacksjs/logsmith": "^0.2.1",
    "@stacksjs/ts-i18n": "^0.0.0",
    "bun-git-hooks": "^0.3.1",
    "bun-plugin-dtsx": "^0.9.13",
    "bun-types": "^1.3.10",
    "bunfig": "^0.15.6",
    "ts-syntax-highlighter": "^0.2.1",
    "typescript": "^5.9.3"
  },
  "git-hooks": {
    "pre-commit": {
      "staged-lint": {
        "*.{js,ts,json,yaml,yml,md}": "bunx --bun pickier --fix"
      }
    },
    "commit-msg": "bunx gitlint --edit .git/COMMIT_EDITMSG"
  }
}
